/**
 * In a real-world application, this should be stored in environment variables.
 * For example, create a .env.local file with:
 * API_KEY=your-secret-api-key
 * And access it via process.env.API_KEY
 */
export const API_KEY = "my-secret-api-key";
